import 'package:flutter/material.dart';

class ContractusPage extends StatelessWidget {
  const ContractusPage({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Parties'),
          centerTitle: true,
          backgroundColor: Colors.pink,
        ),
      );
}
